{
  "name": "payday-app",
  "version": "3.0.0",
  "description": "Personal paycheck calculator — multi-user ready, invite-only",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step'"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "bcryptjs": "^2.4.3",
    "jose": "^5.3.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
